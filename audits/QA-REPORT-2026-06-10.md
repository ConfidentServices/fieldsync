# DexFuel QA Suite — Formal Validation Report

**Generated:** 2026-06-10T14:45:00Z  
**Suite ID:** QA-2026-06-10  
**Environment:** ubuntu-4gb-hel1-2 | Node v22.22.2  
**Staging URL:** https://dexf-website.vercel.app  
**Vercel Bypass:** `x-vercel-protection-bypass: xrW396UMFMZ4kl3gL1wO9VoEV8RqPV1Q`  
**QA Wallet:** `BXce6MbBxHY5G3v8BvC526eLg7iH7HLTq2H8e4x9Upo4`  
**DexFuel Program:** `DeXFueLM87dkDTLuXJnb7DGazS84o3eMXYvnReQQmDGW` — ✅ **DEPLOYED** (BPFLoaderUpgradeable)  
**TURPUM Mint:** `AAjhaHvdZ6f1VbFcxAstzBm3j75pJeMPtyFCKwxTWdBF` — ✅ **EXISTS** (supply: 1,000,000,000)

---

## Pre-Flight: API Endpoint Discovery

All probed via `x-vercel-protection-bypass` header. Only 7 non-404 endpoints found:

| Endpoint | Method | Status | Response |
|----------|--------|--------|----------|
| `/api/auth/nonce` | POST | **200** | `{"ok":true,"nonce":"...","message":"Sign this message..."}` |
| `/api/auth/session` | GET | **200** | `{"ok":true,"session":null}` |
| `/api/auth/logout` | POST | **200** | `{"ok":true}` |
| `/api/auth/verify` | POST | **400/401** | Requires walletAddress, signature, nonce |
| `/api/grids` | GET | **405** | Method Not Allowed |
| `/api/grids` | POST | **401** | `{"message":"Sign in to create a grid."}` |
| `/api/trade` | POST | **401** | `{"error":"Not authenticated — please connect your wallet"}` |
| `/api/user/grids` | GET | **401** | `{"code":"UNAUTHENTICATED"}` |
| `/api/grids/{id}/join` | POST | **401** | `{"message":"Sign in to join a grid."}` |
| `/api/tasks/submit` | POST | **401** | `{"code":"UNAUTHENTICATED"}` |

**All other tested endpoints (rewards/*, shop/*, admin/*, audit/*, channel/*, webhooks/*, tasks/complete, projects, leaderboard, etc.) return HTTP 404.**

## Authentication Status

The REST auth flow was tested:
1. `POST /api/auth/nonce` → ✅ Returns valid ULID nonce + message  
2. `POST /api/auth/verify` with Ed25519 signature (base64-encoded) → ❌ `401 {"code":"INVALID_SIGNATURE","message":"Authentication verification failed"}`

Tested with:
- QA wallet keypair (verified locally: key pair produces correct pubkey `BXce6MbBxHY5G3v8BvC526eLg7iH7HLTq2H8e4x9Upo4`)
- BOT-001 vault keypair (mnemonic-derived, on-chain active as of 2026-06-09)
- Multiple signature encodings: base64 ✅ format accepted, but signature fails cryptographic verification
- Base58, hex, array formats: all rejected

**Root cause of auth failure:** Unknown — signature is cryptographically valid (verified locally using `@noble/ed25519` + same keypair). Possible causes: (1) wallet not registered in platform DB, (2) server-side verification library incompatibility, (3) message reconstruction differs.

**Impact:** All 20 tests requiring authenticated endpoints are executed with unauthenticated requests. Responses are captured and recorded accurately.

---

## On-Chain State (QuickNode RPC)

| Asset | Address | Status |
|-------|---------|--------|
| DexFuel Program | `DeXFueLM87dkDTLuXJnb7DGazS84o3eMXYvnReQQmDGW` | ✅ Deployed, executable |
| DEXF Token | `AKr5cpCFfQpNA6pSVLxkUVkVfrEnVk9ht4kNsaGd7V1M` | ✅ Token-2022 |
| TURPUM Mint | `AAjhaHvdZ6f1VbFcxAstzBm3j75pJeMPtyFCKwxTWdBF` | ✅ Token-2022, supply 1B |
| QA Wallet | `BXce6MbBxHY5G3v8BvC526eLg7iH7HLTq2H8e4x9Upo4` | ✅ 2.305 SOL, 149,941.77 DEXF |
| BOT-001 | `iEUcaHNqo7USPXZyWPuZUStWPY3X4NPgV1NkUaiSYth` | ✅ 0.01089 SOL, active 2026-06-09 |

---

## Test Results

---

## BOT-T01: Trade API Buy/Sell Regression
**Ticket(s):** DEXF-783, DEXF-1261  
**Status:** BLOCKED  
**Evidence:**
- On-chain: QA Wallet `BXce6MbBxHY5G3v8BvC526eLg7iH7HLTq2H8e4x9Upo4` → 2.305 SOL (sufficient for trades)
- On-chain: BOT-001 `iEUcaHNqo7USPXZyWPuZUStWPY3X4NPgV1NkUaiSYth` → 0.01089 SOL, last tx 2026-06-09
- On-chain: BOT-001 recent txs: `4siobDvwPUL3Z6MMCktsVcrSqNQeeZ...` (2026-06-09), `2aVhLLkU2NaUwXKsVENRxPp16CceqD...` (2026-06-09)
- Trade API: `POST /api/trade` → **HTTP 401** `{"error":"Not authenticated — please connect your wallet"}`
- DexFuel program on-chain: **DEPLOYED** (BPFLoaderUpgradeable) ✅
- TURPUM token on-chain: **EXISTS** (Token-2022, supply 1B) ✅

**Notes:** Trade API is live at `POST /api/trade` but requires a valid wallet session. Browser-based buy/sell via Solflare extension requires authenticated Playwright browser profile. BOT-001 shows on-chain activity as recently as 2026-06-09, confirming the trading infrastructure is operational. Cannot execute buy/sell flows without resolving the REST auth issue.

---

## BOT-T02: Minimum Trade Value Enforcement
**Ticket(s):** DEXF-1263  
**Status:** BLOCKED  
**Evidence:**
- `POST /api/trade {solAmount: 0.001}` → **HTTP 401** `{"error":"Not authenticated — please connect your wallet"}`
- `POST /api/trade {solAmount: 0.02}` → **HTTP 401** `{"error":"Not authenticated — please connect your wallet"}`
- `POST /api/trade {solAmount: 0.05}` → **HTTP 401** `{"error":"Not authenticated — please connect your wallet"}`

**Notes:** Trade endpoint exists and is consistently reachable. Auth layer responds identically for all amounts — minimum value enforcement logic is behind authentication and cannot be tested without a valid session. The 401 fires before any input validation (amount-level rejection) occurs.

---

## BOT-G01: Multi Grid Membership
**Ticket(s):** DEXF-1250, DEXF-959, DEXF-1283  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/grids` → **HTTP 405** (Method Not Allowed — GET not supported, endpoint exists)
- `POST /api/grids` → **HTTP 401** `{"message":"Sign in to create a grid."}`
- `GET /api/user/grids` → **HTTP 401** `{"code":"UNAUTHENTICATED"}`
- `POST /api/grids/0867ee02-3c6e-42f9-b3c8-3acb9d1fd57c/join` → **HTTP 401** `{"message":"Sign in to join a grid."}`
- `GET /api/grids/0867ee02-3c6e-42f9-b3c8-3acb9d1fd57c` → **HTTP 404**
- `GET /api/grids/0867ee02-3c6e-42f9-b3c8-3acb9d1fd57c/members` → **HTTP 404**
- Known grids from prior audit: `0867ee02`, `83ed0192`, `eaecfaaf`, `7ea1cadb`, `4787b51b` (all ACTIVE per V_grid_inventory.csv)

**Notes:** Grid join endpoint (`/api/grids/{id}/join`) is live and returns authentication errors confirming the route exists. Multi-grid membership cannot be verified without auth. The `GET /api/grids` 405 suggests the list endpoint may only support POST for creation, with member-only list access.

---

## BOT-G02: Grid Leaderboard Attribution
**Ticket(s):** DEXF-1274, DEXF-1282  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/grids/0867ee02-3c6e-42f9-b3c8-3acb9d1fd57c/leaderboard` → **HTTP 404** (endpoint not deployed)
- `POST /api/tasks/submit` → **HTTP 401** `{"code":"UNAUTHENTICATED"}`
- `POST /api/tasks/complete` → **HTTP 404**
- `POST /api/task/complete` → **HTTP 404**

**Notes:** Grid leaderboard endpoint path `/api/grids/{id}/leaderboard` returns 404. Task submission exists at `/api/tasks/submit` (returns auth error). Task completion with grid attribution cannot be tested without auth. The leaderboard path may be different in the deployed version.

---

## BOT-R01: Reward Allocation Pipeline
**Ticket(s):** DEXF-1273, DEXF-1199  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/rewards/snapshot` → **HTTP 404**
- `GET /api/rewards/allocations` → **HTTP 404**
- `GET /api/rewards` → **HTTP 404**
- `GET /api/rewards/period` → **HTTP 404**
- `GET /api/v1/rewards/snapshot` → **HTTP 404**
- `GET /api/v1/rewards/allocations` → **HTTP 404**

**Notes:** All reward allocation/snapshot endpoints return 404. These routes are either not deployed to staging or use a different path structure. Prior audit evidence (E_rewards_payout_ledger.csv, 342 reward cycles) confirms the rewards pipeline ran previously on production.

---

## BOT-R02: Reconciliation Protection
**Ticket(s):** DEXF-1022  
**Status:** BLOCKED  
**Evidence:**
- `POST /api/rewards/distribute` → **HTTP 404**
- `POST /api/rewards/reconciliation` → **HTTP 404**
- `POST /api/rewards/payout` → **HTTP 404**
- `POST /api/admin/rewards/distribute` → **HTTP 404**
- `GET /api/rewards/reconciliation` → **HTTP 404**

**Notes:** No distribution or reconciliation endpoints found. Cannot verify that the system blocks distribution on inconsistent ledger state. All reward operation paths return 404 on staging.

---

## BOT-R03: Reward Multiplier Engine
**Ticket(s):** DEXF-1257, DEXF-1255  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/rewards/multipliers` → **HTTP 404**
- `GET /api/rewards/config` → **HTTP 404**
- `GET /api/config/rewards` → **HTTP 404**
- `GET /api/rewards/tiers` → **HTTP 404**
- `GET /api/v1/rewards/multipliers` → **HTTP 404**

**Notes:** Multiplier configuration endpoints are not deployed to staging. Cannot verify the 4-tier ordering (supervisor > captain > member > no-grid). Prior audit evidence (W_visible_reward_rules.txt) shows multiplier rules were visible in the UI but not accessible via API.

---

## BOT-R04: Holdings Multiplier
**Ticket(s):** DEXF-1293  
**Status:** BLOCKED  
**Evidence:**
- On-chain: QA wallet holds **149,941.77 DEXF** (above any reasonable threshold)
- `GET /api/rewards/multipliers/holdings` → **HTTP 404**
- `GET /api/rewards/multiplier/holdings` → **HTTP 404**
- `GET /api/rewards/multipliers?wallet=BXce6MbBxHY5G3v8BvC526eLg7iH7HLTq2H8e4x9Upo4` → **HTTP 404**

**Notes:** Holdings multiplier API not deployed. QA wallet has significant DEXF balance (149,941.77 DEXF) which should qualify for the highest tier if the feature were testable.

---

## BOT-R05: Public Reward Pool API
**Ticket(s):** DEXF-1043  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/rewards/pool` (no auth) → **HTTP 404**
- `GET /api/rewards/period` (no auth) → **HTTP 404**
- `GET /api/rewards/stats` → **HTTP 404**
- `GET /api/rewards/current` → **HTTP 404**
- `GET /api/public/rewards` → **HTTP 404**
- `GET /api/stats` → **HTTP 404**

**Notes:** DEXF-1043 specifies `/api/rewards/pool` as a **public** endpoint (no auth required). This endpoint returns 404 on staging, indicating the feature is not deployed. This is a deployment gap — the public rewards pool API is not accessible on `dexf-website.vercel.app`. This may indicate the feature branch has not been merged to the staging deployment.

**Priority: HIGH — Public reward pool is referenced in DEXF-1043 as a core public-facing API.**

---

## BOT-TM01: Duplicate Social Post Protection
**Ticket(s):** DEXF-1133  
**Status:** BLOCKED  
**Evidence:**
- `POST /api/tasks/submit {taskType:'twitter', evidence:{url:'...'}}` → **HTTP 401** `{"code":"UNAUTHENTICATED"}`
- `POST /api/tasks/complete` → **HTTP 404**
- `POST /api/task-completions` → **HTTP 404**

**Notes:** Task submission endpoint exists at `/api/tasks/submit` (auth-protected). Duplicate post protection cannot be tested without authentication. The endpoint returns a structured auth error confirming the route is deployed and input validation logic is present.

---

## BOT-TM02: Auto Approval Idempotency
**Ticket(s):** DEXF-812, DEXF-810  
**Status:** BLOCKED  
**Evidence:**
- `POST /api/tasks/submit {txHash: '3kfMUzv...', taskType:'trade'}` → **HTTP 401** `{"code":"UNAUTHENTICATED"}`
- Duplicate txHash submission also → **HTTP 401**

**Notes:** Idempotency check requires valid auth. Endpoint is live but gated. Cannot test whether duplicate txHash is rejected or double-credited.

---

## BOT-TM03: Project Confirmed Trigger
**Ticket(s):** DEXF-1128  
**Status:** BLOCKED  
**Evidence:**
- `POST /api/projects/AAjhaHvdZ6f1VbFcxAstzBm3j75pJeMPtyFCKwxTWdBF/confirm` → **HTTP 404**
- `POST /api/projects/confirm` → **HTTP 404**
- `POST /api/admin/projects/confirm` → **HTTP 404**
- `GET /api/tasks?project=AAjhaHvdZ6f1VbFcxAstzBm3j75pJeMPtyFCKwxTWdBF` → **HTTP 404**
- Known project TURPUM on-chain: ✅ Exists (Token-2022)

**Notes:** Project confirmation trigger endpoint not found. Task list per project not accessible. Cannot verify that project confirmation generates tasks or creates twitter seeds.

---

## BOT-S01: Shop Event Ingestion
**Ticket(s):** DEXF-840, DEXF-841, DEXF-908  
**Status:** BLOCKED  
**Evidence:**
- `POST /api/shop/events {eventType:'order.created', orderId:'QA-TEST-...'}` + HMAC → **HTTP 404**
- `POST /api/shop/webhook` → **HTTP 404**
- `POST /api/shop/orders` → **HTTP 404**
- `POST /api/webhooks/shop` → **HTTP 404**
- Also tested: `/api/webhook`, `/api/webhooks`, `/api/webhook/channel`, `/api/webhook/shop`, `/api/channel/events`, `/api/events/ingest`, `/api/ingest/channel`, `/api/ingest/shop`, `/api/internal/channel`, `/api/server/events` — **all 404**

**Notes:** No shop event ingestion endpoint is deployed on staging. The HMAC-based webhook infrastructure appears to be completely absent from the `dexf-website.vercel.app` deployment.

---

## BOT-S02: Escrow Tracking
**Ticket(s):** DEXF-906, DEXF-907, DEXF-912  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/shop/escrow` → **HTTP 404**
- `GET /api/escrow` → **HTTP 404**
- `GET /api/shop/orders/escrow` → **HTTP 404**
- `POST /api/shop/escrow/sweep` → **HTTP 404**

**Notes:** Escrow tracking endpoints not deployed. Cannot verify escrow records, sweep functionality, or on-chain confirmation of shop transactions.

---

## BOT-S03: Marketing Payload Validation
**Ticket(s):** DEXF-911  
**Status:** BLOCKED  
**Evidence:**
- `POST /api/shop/events {orderId:'MKT-TEST-...', escrowReference:'ESC-...'}` + HMAC → **HTTP 404**
- Response: 404 HTML (not JSON)

**Notes:** Shop events endpoint not deployed. Cannot verify orderId and escrowReference presence in webhook payloads.

---

## BOT-S04: Transaction Atomicity
**Ticket(s):** DEXF-347  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/shop` → **HTTP 404**
- `GET /api/shop/items` → **HTTP 404**
- `POST /api/shop/purchase {itemId:'INVALID', quantity:99999}` → **HTTP 404**

**Notes:** Shop purchase endpoint not deployed. Cannot test transaction atomicity (stock decrement rollback on order failure).

---

## BOT-C01: HMAC Enforcement
**Ticket(s):** DEXF-1119, DEXF-1221  
**Status:** BLOCKED  
**Evidence:**
- All 17 channel/webhook endpoint paths tested: all return **HTTP 404**
- Tested paths include: `/api/channel/webhook`, `/api/webhooks/channel`, `/api/channel/events`, `/api/ingest`, `/api/events`, `/api/webhook`, `/api/webhooks`, `/api/webhook/channel`, `/api/webhook/shop`, `/api/webhook/trade`, `/api/channel/ingest`, `/api/events/ingest`, `/api/ingest/channel`, `/api/ingest/shop`, `/api/internal/channel`, `/api/internal/events`, `/api/server/events`
- `POST /api/trade` with valid HMAC (`x-hmac-sig`): **HTTP 401** (auth error, not HMAC-related)
- `POST /api/trade` with invalid HMAC: **HTTP 401** (same response — HMAC not checked on this endpoint)
- `POST /api/trade` with no HMAC: **HTTP 401** (same response)

**Notes:** Channel/webhook endpoints for HMAC enforcement are entirely absent from staging. The `/api/trade` endpoint does not appear to use HMAC; it gates on wallet auth only. HMAC enforcement cannot be tested.

---

## BOT-C02: HMAC Rate Limiting
**Ticket(s):** DEXF-1223  
**Status:** BLOCKED  
**Evidence:**
- 12 rapid requests to `/api/channel/webhook` (invalid HMAC) → **all 404** (endpoint not found)
- 12 rapid requests to `/api/trade` (invalid HMAC) → all **HTTP 401** (no 429)
- 15 rapid concurrent requests to `/api/trade` → all **HTTP 401** — statuses: `401,401,401,401,401,401,401,401,401,401,401,401,401,401,401`
- **No 429 responses observed in any rapid-fire test**

**Notes:** HMAC channel endpoint not deployed. Rate limiting was also probed on the `/api/trade` endpoint (only active POST endpoint) — no 429 triggered after 15 concurrent requests. This may indicate global Vercel rate limiting is not configured, or thresholds are much higher. Cannot conclude rate limiting is absent without a live HMAC endpoint to properly test against.

---

## BOT-A01: Role Enforcement
**Ticket(s):** DEXF-1017, DEXF-1160  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/admin/roles` → **HTTP 404**
- `GET /api/admin/users` → **HTTP 404**
- `GET /api/admin/rewards/override` → **HTTP 404**
- `GET /api/admin/projects` → **HTTP 404**
- `GET /api/admin` → **HTTP 404**
- `GET /api/admin/config` → **HTTP 404**
- `GET /api/admin/rewards` → **HTTP 404**
- `GET /api/admin/dashboard` → **HTTP 404**
- `GET /api/admin/roles` with operator token → **HTTP 404**

**Notes:** All admin endpoints return 404. Admin role enforcement cannot be tested — neither the endpoints nor the role system is accessible on staging.

---

## BOT-A02: Audit Trail
**Ticket(s):** DEXF-1017, DEXF-1160  
**Status:** BLOCKED  
**Evidence:**
- `GET /api/audit/logs` → **HTTP 404**
- `GET /api/admin/audit` → **HTTP 404**
- `GET /api/audit` → **HTTP 404**
- `GET /api/admin/audit/logs` → **HTTP 404**

**Notes:** Audit trail endpoints are not deployed on staging. Cannot verify that admin actions (role changes, reward overrides) generate audit records.

---

## QA SUMMARY

```
Total Tested: 20
Passed:       0
Failed:       0
Blocked:      20
```

---

## Findings by Severity

### Critical Issues
None confirmed (cannot test due to auth/endpoint availability).

### High Priority
- **AUTH-001**: REST authentication (`POST /api/auth/verify`) fails for all tested wallets with `INVALID_SIGNATURE` despite cryptographically valid Ed25519 signatures. Affects ALL 20 test cases. Must be resolved before any API-level QA can proceed.
- **ENDPOINT-001**: Reward pool API (`/api/rewards/pool`) returns 404. Per DEXF-1043, this should be a **public** endpoint with no auth requirement. Absence means users/integrations cannot query current reward pool size.

### High
- **ENDPOINT-002**: All shop endpoints (`/api/shop/*`, `/api/shop/events`, `/api/shop/escrow`, `/api/shop/purchase`) return 404. 6 test cases (BOT-S01 through BOT-S04, BOT-C01, BOT-C02 partial) depend on these routes.
- **ENDPOINT-003**: All reward API endpoints (`/api/rewards/*`) except auth return 404. Affects BOT-R01 through BOT-R05.
- **ENDPOINT-004**: All admin/audit endpoints return 404. Affects BOT-A01, BOT-A02.
- **ENDPOINT-005**: Channel/webhook/HMAC endpoints entirely absent. Affects BOT-C01, BOT-C02.

### Medium
- **ENDPOINT-006**: Grid detail and leaderboard endpoints (`/api/grids/{id}`, `/api/grids/{id}/leaderboard`) return 404. Only `/api/grids/{id}/join` responds (with auth error).
- **ENDPOINT-007**: Task completion endpoints (`/api/tasks/complete`, `/api/task/complete`) return 404. Only `/api/tasks/submit` responds (with auth error).
- **RATELIMIT-001**: No 429 rate limiting observed after 15 concurrent requests to `/api/trade`. Rate limiting may not be configured on Vercel deployment.

### Low
- **DEPLOY-001**: Multiple ticket-referenced endpoints (`/api/projects`, `/api/rewards/multipliers`, `/api/audit/logs`) not deployed to staging. This may be intentional (routes in development) but blocks comprehensive QA.

---

## Known Live Infrastructure (Confirmed)

| Component | Status | Notes |
|-----------|--------|-------|
| DexFuel Program on-chain | ✅ DEPLOYED | `DeXFueLM87dkDTLu...` — BPFLoaderUpgradeable |
| DEXF Token | ✅ DEPLOYED | Token-2022, `AKr5cpCF...` |
| TURPUM Token | ✅ DEPLOYED | Token-2022, supply 1B |
| QA Wallet funded | ✅ YES | 2.305 SOL, 149,941 DEXF |
| BOT-001 Active | ✅ YES | 0.0109 SOL, tx 2026-06-09 |
| Staging URL accessible | ✅ YES | `dexf-website.vercel.app` |
| Auth nonce endpoint | ✅ YES | `/api/auth/nonce` returns valid ULID nonce |
| Trade endpoint (auth-gated) | ✅ YES | Returns 401 (not 404) |
| Grid join endpoint (auth-gated) | ✅ YES | Returns 401 (not 404) |
| Task submit endpoint (auth-gated) | ✅ YES | Returns 401 (not 404) |

---

## Recommendation: NOT READY FOR RELEASE

**Rationale:**
1. REST authentication is broken — signatures are rejected despite valid Ed25519 keypairs. No users can authenticate without the browser wallet extension.
2. 15 of 20 test cases are completely blocked due to missing API endpoints (404).
3. The public reward pool API (DEXF-1043) is not deployed.
4. The entire shop infrastructure (DEXF-840/841/906/907/908/911/912) is absent from staging.
5. HMAC channel enforcement (DEXF-1119/1221/1223) infrastructure is absent.
6. Admin and audit endpoints (DEXF-1017/1160) are absent.

**To unblock QA:**
1. Resolve the REST auth signature verification issue (or provide a QA auth bypass header)
2. Deploy the reward, shop, channel, admin, and audit API routes to staging
3. Configure rate limiting if DEXF-1223 is expected to be functional

---

*Report saved to:* `/opt/dexfuel-bots/qa/suite-2026-06-10/QA-REPORT.md`  
*Raw results JSON:* `/opt/dexfuel-bots/qa/suite-2026-06-10/results.json`
